function transient(opts)
% TRANSIENT  Transient response due to a collision.

%% Imports

% Directory where the present file lies.
file_dir = fileparts(mfilename("fullpath"));

% Constants and bare structure data.
BS = load(fullfile(file_dir, "../res/bare_struct.mat"));
KM = load(fullfile(file_dir, "../res/modeling_mat"));

	function set_damping_matrix()
		% SET_DAMPING_MATRIX  Set the


		


	end

end